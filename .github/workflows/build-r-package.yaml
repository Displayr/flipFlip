name: Build flipFlip

on:
  workflow_dispatch:
  push:

jobs:
  build:
    name: Build flipFlip
    permissions:
      checks: write
      contents: read
    uses: Displayr/nixr-public/.github/workflows/build-flake-installable.yaml@master
    with:
      installable-path: pkgs.x86_64-linux.rPackages.flipFlip
      build-args: --override-input flipFlip .
      installable-name: flipFlip
    secrets: inherit
